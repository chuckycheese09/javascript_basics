/**
 * 
 * @param {[]} array - the array for which we are getting the first element
 * @returns {} first element in an array
 */
let firstElement = (array) => {
    return array[0];
}
console.log(firstElement([10, 20, 30, 40]));

